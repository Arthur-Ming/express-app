export enum PostsSortDirection {
  asc = 'asc',
  desc = 'desc',
}

export enum PostsQueryParamsEnum {
  sortBy = 'sortBy',
  sortDirection = 'sortDirection',
  pageNumber = 'pageNumber',
  pageSize = 'pageSize',
}
