export enum CommentsBodyFieldsEnum {
  content = 'content',
}

export enum CommentsSortDirection {
  asc = 'asc',
  desc = 'desc',
}

export enum CommentsQueryParamsEnum {
  sortBy = 'sortBy',
  sortDirection = 'sortDirection',
  pageNumber = 'pageNumber',
  pageSize = 'pageSize',
}
