export enum BlogsSortDirection {
  asc = 'asc',
  desc = 'desc',
}

export enum BlogsQueryParamsEnum {
  searchNameTerm = 'searchNameTerm',
  sortBy = 'sortBy',
  sortDirection = 'sortDirection',
  pageNumber = 'pageNumber',
  pageSize = 'pageSize',
}
